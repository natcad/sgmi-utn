const path = require('path');

module.exports = {
  'config': path.resolve('src','config', 'config.cjs'),   // archivo de config DB
  'models-path': path.resolve('src', 'models'),    // acá van los modelos
  'seeders-path': path.resolve('src', 'seeders'),
  'migrations-path': path.resolve('src', 'migrations'),
};
